#Exercises - Day 13
print()

# 1 - Explica la diferencia entre map, filter y reduce.

"""
    La función map(), itera por todos los elementos del iterable, aplicando la función a cada uno de ellos, y crea una nueva lista con los resultados.
    La función filter(), itera por todos los elementos del iterable, y crea una nueva lista sólo con los elementos que hayan pasado el criterio de filtro.
    la función reduce(), itera por todos los elementos, pero a diferencia de map, devuelve un único valor.

    La principal diferencia entre estos 3, es la manera a tratar los datos. Mientras que map crea una nueva lista con todos los elementos iterados ya procesados,
    la función filter() sólo devuelve los items que cumplan el criterio, y reduce() va aplicando la función al resultado de los dos iterables anteriores.
"""
